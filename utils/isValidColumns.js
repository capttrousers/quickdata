/*

  this will export a function that takes a single argument, an array of column objects
  and parses the columns array and checks for all appropriate properties

  the function can also do appropriate clipping of props
